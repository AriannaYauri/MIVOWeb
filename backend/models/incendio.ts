export interface Incendio {
    id: string; // "Diario", "Semanal" o "Mensual"
    date: string; // Fecha del incendio en formato ISO (YYYY-MM-DD)
    count: number; // Cantidad de incendios detectados
  }
  